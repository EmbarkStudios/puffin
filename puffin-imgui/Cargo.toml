[package]
name = "puffin-imgui"
version = "0.11.0"
authors = ["Embark <opensource@embark-studios.com>"]
description = "ImGui GUI bindings for the Puffin profiler"
license = "MIT OR Apache-2.0"
edition = "2018"
homepage = "https://github.com/EmbarkStudios/puffin"
repository = "https://github.com/EmbarkStudios/puffin"
readme = "README.md"
categories = ["development-tools::profiling", "gui"]
keywords = ["profiler", "instrumentation", "gamedev"]
include = ["**/*.rs", "Cargo.toml"]

[dependencies]
imgui = { version = "0.7" }
natord = "1.0.9"
puffin = { version = "0.8.1", path = "../puffin" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
glium = { version = "0.29", default-features = true }
imgui-glium-renderer = "0.7"
imgui-winit-support = { version = "0.7", default-features = false, features = [
    "winit-24",
] }
